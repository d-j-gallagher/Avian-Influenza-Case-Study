# Table 1 — Human influenza incidence and mortality by province, 2024

This table is stored as a CSV so you can reuse it for calculations later.

- File: `content/data/part_a_table_1.csv`

In the app, it can be displayed as an interactive table (scrollable/searchable) even in Option A.
